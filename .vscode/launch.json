{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "${userHome}/Downloads/wingmate-sandbox",
        "--extensionDevelopmentPath=${workspaceFolder}",
      ],
      "preLaunchTask": "build"
    }
  ]
}
